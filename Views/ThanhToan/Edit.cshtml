@model HotelManagement.Models.ThanhToan
@{
    ViewData["Title"] = "Cập nhật thanh toán";
}

<h2 class="mb-4">@ViewData["Title"]</h2>

<form asp-action="Edit" asp-route-id="@Model.maThanhToan" method="post" class="needs-validation" novalidate>
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="maThanhToan" />
    <partial name="_Form" />
</form>
